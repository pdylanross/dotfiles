- clean: ['~']

- link:
    ~/.gitconfig:
        path: gitconfig
    ~/.zshrc:
        path: zshrc
    ~/.antigen/antigen.zsh:
        path: antigen/antigen.zsh
        create: true
    ~/.p10k.zsh:
        path: p10k.zsh
        

- shell:
  - chsh -s $(which zsh)

