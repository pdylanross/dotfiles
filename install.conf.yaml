- clean: ['~']

- link:
    ~/.gitconfig:
        path: gitconfig
    ~/.zshrc:
        path: zshrc
    ~/.antigen/antigen.zsh:
        path: antigen/antigen.zsh
        create: false

- shell:
  - chsh -s $(which zsh)

